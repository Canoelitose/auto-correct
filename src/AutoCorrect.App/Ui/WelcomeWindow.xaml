<Window x:Class="AutoCorrect.App.Ui.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520"
        Height="430"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True"
        Topmost="True"
        Background="#FFFDFDFD"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="Segoe UI"
        FontSize="13">

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock x:Name="HeadlineText" FontSize="19" FontWeight="SemiBold" TextWrapping="Wrap" />
            <TextBlock x:Name="IntroText" TextWrapping="Wrap" Margin="0,10,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <Border Padding="12,10" CornerRadius="6" Background="#FFF4F4F5">
                <StackPanel>
                    <TextBlock x:Name="HotkeyCaption" FontSize="11" Foreground="#FF71717A" />
                    <TextBlock x:Name="HotkeyText" FontSize="17" FontWeight="SemiBold" Margin="0,2,0,0" />
                </StackPanel>
            </Border>

            <TextBlock x:Name="TrayHintText"
                       TextWrapping="Wrap"
                       Margin="0,14,0,0"
                       FontSize="12"
                       Foreground="#FF3F3F46" />

            <Border x:Name="EnginePanel"
                    Margin="0,14,0,0"
                    Padding="12,10"
                    CornerRadius="6"
                    Background="#FFFEF2F2">
                <TextBlock x:Name="EngineText"
                           TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="#FFB91C1C" />
            </Border>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button x:Name="SettingsButton" Padding="14,6" Margin="0,0,8,0" />
            <Button x:Name="CloseButton" Padding="18,6" IsDefault="True" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
