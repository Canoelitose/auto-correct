<Window x:Class="AutoCorrect.App.Ui.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Über AutoCorrect"
        Width="460"
        Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#FFFDFDFD"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="Segoe UI"
        FontSize="13">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="AutoCorrect" FontSize="20" FontWeight="SemiBold" />
            <TextBlock x:Name="VersionText" Foreground="#FF71717A" Margin="0,2,0,0" />
            <TextBlock x:Name="TaglineText"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,14,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock x:Name="DetailsText" TextWrapping="Wrap" FontSize="12" Foreground="#FF3F3F46" />
                <TextBlock x:Name="NoteHeader"
                           FontWeight="SemiBold"
                           Margin="0,14,0,4" />
                <TextBlock x:Name="NoteText"
                           TextWrapping="Wrap"
                           FontSize="12"
                           Foreground="#FF3F3F46" />
            </StackPanel>
        </ScrollViewer>

        <Button x:Name="CloseButton"
                Grid.Row="2"
                HorizontalAlignment="Right"
                Margin="0,16,0,0"
                Padding="18,6"
                IsDefault="True"
                IsCancel="True" />
    </Grid>
</Window>
